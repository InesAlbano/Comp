public void printd(number d)
public integer atoi(string s)
public void println()

public number pFatorial(integer factor){
  number result := fatorial(factor);
  pFatorial := result
};

public integer entry (integer argc, string *argv, string *envp) {
  if argc > 2 then {
    printd(pFatorial((atoi(argv[1])))
    println()
  } else {
    printd(pFatorial(5))
    println()
  }
  entry := 0
};
